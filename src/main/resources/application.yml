spring:
  application:
    name: test-microservice

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

app:
  service-name: test-microservice
  environment: local
